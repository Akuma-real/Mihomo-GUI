# /etc/sysconfig/mihomo — service environment
# 修改以下变量以指定配置目录与配置文件；修改后需：
#   sudo systemctl daemon-reload && sudo systemctl restart mihomo

# 配置目录（用于相对路径资源、外置 UI 等）
MIHOMO_DIR="/var/lib/mihomo"

# 配置文件（可填绝对路径或相对 MIHOMO_DIR 的文件名）
# 留空表示未设置（服务将拒绝启动，直到你指定）
MIHOMO_CONFIG=""

# 额外启动参数（例如：--disable-color）
MIHOMO_EXTRA_ARGS=""
